# إعداد الأمان — ثانوية مارز الإيمان

النظام الآن محميّ بـ **Firebase Authentication** (تسجيل دخول مشفّر) و**قواعد أمان Firestore**.
كلمات المرور لا تُخزَّن في قاعدة البيانات إطلاقاً — يديرها Firebase مشفّرة.

نفّذ الخطوات التالية **مرة واحدة** من [Firebase Console](https://console.firebase.google.com/project/imam-warsh).

## 1) تفعيل تسجيل الدخول بالبريد وكلمة المرور
- Authentication → **Get started** → تبويب **Sign-in method**.
- فعّل مزوّد **Email/Password** → Save.

## 2) تفعيل Firestore Database
- Firestore Database → **Create database** → اختر موقعاً قريباً → ابدأ.

## 3) نشر قواعد الأمان
- Firestore Database → تبويب **Rules**.
- الصق محتوى ملف [`firestore.rules`](firestore.rules) بالكامل → **Publish**.

## 4) إنشاء حساب المدير الأول (مرة واحدة فقط)
النظام يحوّل «اسم المستخدم» إلى بريد داخلي بالصيغة: `<اسم_المستخدم>@maraz-aliman.app`

**أ. أنشئ مستخدم المصادقة:**
- Authentication → **Users** → **Add user**.
- Email: `admin@maraz-aliman.app`
- Password: اختر كلمة مرور قوية (6 خانات فأكثر).
- بعد الإنشاء، انسخ **User UID**.

**ب. أنشئ ملف صلاحياته:**
- Firestore Database → **Start collection** → Collection ID: `maraz_users`.
- Document ID: الصق **UID** المنسوخ.
- أضف الحقول (كلها نصية / string):
  - `username` = `admin`
  - `name` = `مدير الثانوية`
  - `role` = `manager`
- احفظ.

## 5) الدخول
افتح المنصة وسجّل الدخول باسم `admin` وكلمة المرور اللي اخترتها.
بعدها يقدر المدير ينشئ بقية الحسابات (وكلاء/معلمين) من صفحة **الحسابات** داخل المنصة — بأمان وبدون خروجه من جلسته.

## ملاحظات أمنية
- **حذف حساب** من داخل المنصة يمنع دخوله فوراً (يحذف ملف صلاحياته). لإزالته نهائياً احذفه أيضاً من Authentication.
- إعدادات Firebase الظاهرة في الكود **ليست سرّاً** — الحماية الحقيقية من قواعد الأمان أعلاه.
- تقوية إضافية مستقبلية: تقسيم البيانات لمجموعات منفصلة لتقييد الكتابة حسب الدور (مثلاً المعلم يكتب الحضور فقط).
